<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ============================================================
         PERMISSION MODEL: Strictly NO root, NO system-level hacks.
         All permissions are standard Android public APIs.
         ============================================================ -->

    <!-- Bluetooth (standard, no root) -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <!-- Android 12+ Bluetooth -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />

    <!-- Location (BLE scanning requires it on Android < 12; optional on 12+) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- System overlay for popup (user-granted via Settings) -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Notifications (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Foreground service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />

    <!-- Wake lock for scan duty cycle -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Receive boot completed for service restart -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- ============================================================
         ASSERTION: NO root permissions requested.
         The following are EXPLICITLY NOT requested:
         - android.permission.ACCESS_SUPERUSER
         - android.permission.WRITE_SECURE_SETTINGS
         - android.permission.SYSTEM_ALERT_WINDOW (granted by user, not system)
         ============================================================ -->

    <!-- Feature declarations -->
    <uses-feature android:name="android.hardware.bluetooth" android:required="true" />
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />
    <uses-feature android:name="android.hardware.location" android:required="false" />

    <application
        android:name=".app.AirPodsApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="Pod Control"
        android:supportsRtl="true"
        android:theme="@style/Theme.AirPodsControl"
        android:resizeableActivity="true"
        tools:targetApi="34">
        <!-- Shizuku Provider - for Shizuku app to discover this app -->
        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:enabled="true"
            android:exported="true"
            android:multiprocess="false" />


        <!-- ===== Main Activity ===== -->
        <activity
            android:name=".app.MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ===== Foreground Service (background persistence) ===== -->
        <service
            android:name="com.airpods.control.core.service.AirPodsService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice">
            <!-- Restart after boot -->
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </service>

        <!-- ===== Bluetooth Broadcast Receiver ===== -->
        <receiver
            android:name="com.airpods.control.core.service.BluetoothStateReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.bluetooth.adapter.action.STATE_CHANGED" />
                <action android:name="android.bluetooth.device.action.ACL_CONNECTED" />
                <action android:name="android.bluetooth.device.action.ACL_DISCONNECTED" />
                <action android:name="android.bluetooth.device.action.BOND_STATE_CHANGED" />
            </intent-filter>
        </receiver>
        <!-- Shizuku GATT Service (runs in :gatt process with shell UID) -->
        <service
            android:name="com.airpods.control.core.bluetooth.shizuku.ShizukuGattService"
            android:exported="true"
            android:process=":gatt">
            <intent-filter>
                <action android:name="rikka.shizuku.client.action.BIND_USER_SERVICE" />
            </intent-filter>
        </service>

    </application>

</manifest>
